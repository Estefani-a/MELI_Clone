//
//  DetailViewModelTests.swift
//  AppIosW6CodePatrollExampleTests
//
//  Created by Estefania Sassone on 24/10/2022.
//  Copyright © 2022 MercadoLibre. All rights reserved.
//

import XCTest
@testable import AppIosW6CodePatrollExample

/*class DetailViewModelTests: XCTestCase {
    
    let mockCategory = "1"
    
    override func setUpWithError() throws {
    }
    
    override func tearDownWithError() throws {
        // Put teardown code here. This method is called after the invocation of each test method in the class.
    }
    
    func testGetDescription() throws {
        let localStorage = MockMotorcycleStorage(ListModel.mock)
        let remoteStorage = MockMotorcycleStorage()
        
        let viewModel = DetailViewModel(remoteService: remoteStorage, localService: localStorage)
        
        let description: () = viewModel.getDescription(item: ListModel.mock.results.description)
        XCTAssertNotNil(description)
    }
    
    
    func testGetGenericList() throws {
        let localStorage = MockMotorcycleStorage(ListModel.mock)
        let remoteStorage = MockMotorcycleStorage()
        let result = ListViewModel(remoteService: remoteStorage, localService: localStorage)
        let list: () = result.getData(category: mockCategory)
        XCTAssertNotNil(list)
        
    }
    
    
}*/
